import layout from "../../layouts/profile/profile";
import profilePasswordForm from "../../components/forms/profile-password-form/profile-password-form";

export default () => {
  return layout(profilePasswordForm());
};
